const menu=document.querySelector('.menu'),nav=document.querySelector('nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const dialog=document.querySelector('#catalogue'),dialogImage=dialog.querySelector('img');document.querySelectorAll('[data-page]').forEach(button=>button.addEventListener('click',()=>{const page=String(button.dataset.page).padStart(2,'0');dialogImage.src=`assets/page-${page}.jpg`;dialogImage.alt=`AllSafe catalogue page ${button.dataset.page}`;dialog.showModal()}));dialog.querySelector('.close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});
document.querySelector('#enquiry').addEventListener('submit',e=>{e.preventDefault();const data=new FormData(e.currentTarget),subject=encodeURIComponent(`Website enquiry from ${data.get('company')||data.get('name')}`),body=encodeURIComponent(`Name: ${data.get('name')}\nCompany: ${data.get('company')}\nEmail: ${data.get('email')}\n\nRequirement:\n${data.get('message')}`);document.querySelector('.form-note').textContent='Opening your email application…';location.href=`mailto:sales@allsafefactory.net?subject=${subject}&body=${body}`});document.querySelector('#year').textContent=new Date().getFullYear();
